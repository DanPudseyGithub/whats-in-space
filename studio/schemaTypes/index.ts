import {siteSettings} from './siteSettings'
import {hero} from './hero'
import {richText} from './richText'
import {page} from './page'

export const schemaTypes = [siteSettings, hero, richText, page]
